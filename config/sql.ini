NEW_KEY=Test
RELEASES=CREATE TABLE IF NOT EXISTS RELEASES (id INT UNSIGNED NOT NULL AUTO_INCREMENT, description VARCHAR(64) NOT NULL, start DATE NOT NULL, end DATE NOT NULL, PRIMARY KEY(id))
PROJECT=CREATE TABLE IF NOT EXISTS Project (name VARCHAR(64), description VARCHAR(256), PRIMARY KEY (name))
